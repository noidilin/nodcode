{
  "name": "@nodcode/database",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "db:generate": "bunx prisma generate",
    "db:migrate:deploy": "bunx prisma migrate deploy",
    "db:migrate:resolve:init": "bunx prisma migrate resolve --applied 20260611000000_init_sessions",
    "db:migrate:status": "bunx prisma migrate status",
    "db:push": "bunx prisma db push"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "prisma": "^7.7.0"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7.7.0",
    "dotenv": "^17.4.1"
  }
}
